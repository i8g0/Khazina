# Demo Cache (TEMPORARY)

Local-only cache storage for hackathon presentations.

**Do not commit** `responses/`, `binary/`, `ai/`, `manifest.json`, or `dataset/` — they are gitignored.

Warm this folder with:

```bash
python scripts/demo/warm_demo_cache.py
```

See `docs/DEMO_CACHE_MODE.md` for full instructions.
