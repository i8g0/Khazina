# Used by Next.js at build time (Docker) and dev server (local)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Dev/hackathon: auto-login on startup (default ON — set false to show login page)
# NEXT_PUBLIC_DEMO_AUTOLOGIN=false
# NEXT_PUBLIC_DEMO_EMAIL=demo@khazina.sa
# NEXT_PUBLIC_DEMO_PASSWORD=DemoExec2026!
